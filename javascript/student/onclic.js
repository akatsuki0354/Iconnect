function Present() {
    const text = document.getElementById("Present")

    if (text.classList.contains('hide')) {
      text.classList.remove('hide')
      text.classList.add('show')
    }
    else {
      text.classList.remove('show')
      text.classList.add('hide')
    }

  }

  function Absent() {
    const text = document.getElementById("Absent")

    if (text.classList.contains('hide')) {
      text.classList.remove('hide')
      text.classList.add('show')
    }
    else {
      text.classList.remove('show')
      text.classList.add('hide')
    }

  }

  function Excuse() {
    const text = document.getElementById("Excuse")

    if (text.classList.contains('hide')) {
      text.classList.remove('hide')
      text.classList.add('show')
    }
    else {
      text.classList.remove('show')
      text.classList.add('hide')
    }

  }

  function Late() {
    const text = document.getElementById("Late")

    if (text.classList.contains('hide')) {
      text.classList.remove('hide')
      text.classList.add('show')
    }
    else {
      text.classList.remove('show')
      text.classList.add('hide')
    }

  }